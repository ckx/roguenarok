hugel,96,140,0	script	hugel420	45,1,1,{
	OnTouch_:
		// If an instance already exists, re-enter it
		if(instance_id(IM_CHAR) != 0) {
			instance_enter(@instname$,-1,-1,getcharid(0),instance_id(IM_CHAR));
			end;
		} 
		CreateFloor0;
	}